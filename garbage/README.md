# Garbage observation inbox

Append tiny independent `grey-garbage-observation/v0` TOML observations here when probable cleanup is encountered incidentally.

Contract and collector behavior are owned by `greygoody/garbage-collector`. Observations are evidence only, not cleanup authority. Everything in this public repository must remain public-safe.
